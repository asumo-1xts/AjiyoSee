[project]
name = "ajiyosee"
version = "0.1.0"
description = "つくば市の飲食店「あじよし」の営業状況をざっくりリアルタイムで可視化する非公認プロジェクト。"
readme = "README.md"
requires-python = ">=3.14"

[tool.ruff]
preview = true
line-length = 80

[dependency-groups]
dev = ["dotenv>=0.9.9", "pytz>=2025.2", "requests>=2.32.5"]
